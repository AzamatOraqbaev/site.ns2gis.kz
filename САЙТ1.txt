<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Musetti | Меню</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-bg: #ffffff;
            --color-card: #f8f9fa;
            --color-primary: #2d2d2d;
            --color-secondary: #6c757d;
            --color-accent: #EA1E1D;
            --color-border: #e9ecef;
            --color-success: #28a745;
            --shadow: 0 4px 20px rgba(234, 30, 29, 0.1);
            --transition: 0.3s ease;
        }

        * { 
            box-sizing: border-box; 
            margin: 0; 
            padding: 0; 
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--color-bg);
            color: var(--color-primary);
            line-height: 1.6;
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        header {
            background: var(--color-bg);
            padding: 15px 0;
            border-bottom: 2px solid var(--color-accent);
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            box-shadow: 0 2px 10px rgba(234, 30, 29, 0.1);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            text-align: center;
        }

        .logo h1 {
            font-size: 2rem;
            font-weight: 700;
            color: var(--color-accent);
            letter-spacing: 1px;
        }

        .logo .subtitle {
            font-size: 0.8rem;
            color: var(--color-secondary);
            margin-top: 2px;
            font-weight: 400;
        }

        nav {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            justify-content: center;
            flex-grow: 1;
            margin: 0 20px;
        }

        nav a {
            text-decoration: none;
            color: var(--color-primary);
            font-weight: 500;
            font-size: 0.8rem;
            padding: 6px 12px;
            border: 1px solid var(--color-border);
            border-radius: 15px;
            transition: all var(--transition);
            background: var(--color-card);
        }

        nav a:hover, nav a.active {
            background-color: var(--color-accent);
            color: var(--color-bg);
            border-color: var(--color-accent);
            transform: translateY(-1px);
        }

        .cart {
            background: var(--color-accent);
            color: var(--color-bg);
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 0.9rem;
            white-space: nowrap;
            border: 2px solid var(--color-accent);
        }

        /* Main Content */
        main {
            padding: 120px 0 60px;
        }

        .menu-category {
            margin-bottom: 60px;
            scroll-margin-top: 80px;
        }

        .menu-category h2 {
            font-size: 1.6rem;
            font-weight: 600;
            margin-bottom: 30px;
            color: var(--color-accent);
            text-align: center;
            position: relative;
            padding-bottom: 10px;
        }

        .menu-category h2:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 2px;
            background: var(--color-accent);
        }

        .menu-items {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 20px;
        }

        .menu-item {
            background: var(--color-card);
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid var(--color-border);
            transition: all var(--transition);
            position: relative;
        }

        .menu-item:hover {
            transform: translateY(-3px);
            border-color: var(--color-accent);
            box-shadow: var(--shadow);
        }

        .menu-item-image {
            width: 100%;
            height: 200px;
            background: linear-gradient(45deg, #f8f9fa, #e9ecef);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--color-accent);
            font-size: 2.5rem;
            border-bottom: 1px solid var(--color-border);
        }

        .menu-details {
            padding: 20px;
        }

        .menu-details h3 {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 8px;
            color: var(--color-primary);
        }

        .menu-details .description {
            font-size: 0.85rem;
            color: var(--color-secondary);
            margin-bottom: 15px;
            line-height: 1.5;
        }

        .item-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .price {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--color-accent);
        }

        .add-btn {
            background: var(--color-success);
            border: none;
            color: white;
            padding: 8px 16px;
            cursor: pointer;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.85rem;
            transition: all var(--transition);
        }

        .add-btn:hover {
            background: #218838;
            transform: translateY(-1px);
        }

        /* Breakfast Special Section */
        .breakfast-special {
            background: linear-gradient(135deg, var(--color-accent) 0%, #c41818 100%);
            color: white;
            padding: 40px 0;
            margin-bottom: 60px;
            border-radius: 15px;
        }

        .breakfast-special .container {
            text-align: center;
        }

        .breakfast-special h2 {
            font-size: 2rem;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .breakfast-special p {
            font-size: 1.1rem;
            opacity: 0.9;
            margin-bottom: 20px;
        }

        .breakfast-time {
            background: rgba(255,255,255,0.2);
            padding: 10px 20px;
            border-radius: 25px;
            display: inline-block;
            font-weight: 600;
        }

        /* Footer */
        footer {
            background: var(--color-primary);
            color: var(--color-bg);
            padding: 40px 0;
            margin-top: 60px;
        }

        .footer-content {
            text-align: center;
        }

        .footer-content h3 {
            color: var(--color-accent);
            margin-bottom: 20px;
            font-weight: 600;
        }

        .contact-info {
            margin-bottom: 8px;
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .hours {
            opacity: 0.8;
            font-style: italic;
            font-size: 0.9rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                padding: 0 15px;
            }

            .header-content {
                flex-direction: column;
                gap: 12px;
            }

            nav {
                margin: 8px 0;
                gap: 4px;
            }

            nav a {
                font-size: 0.75rem;
                padding: 5px 10px;
            }

            .logo h1 {
                font-size: 1.6rem;
            }

            .menu-items {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            .menu-details {
                padding: 15px;
            }

            main {
                padding: 140px 0 40px;
            }

            .menu-category {
                margin-bottom: 50px;
            }

            .breakfast-special {
                padding: 30px 0;
                margin-bottom: 40px;
            }

            .breakfast-special h2 {
                font-size: 1.6rem;
            }
        }

        @media (max-width: 480px) {
            .logo h1 {
                font-size: 1.4rem;
            }

            nav a {
                font-size: 0.7rem;
                padding: 4px 8px;
            }

            .menu-item-image {
                height: 160px;
            }

            .cart {
                font-size: 0.8rem;
                padding: 6px 12px;
            }

            .breakfast-special h2 {
                font-size: 1.4rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <h1>MUSETTI</h1>
                    <div class="subtitle">COFFEE & BAKERY</div>
                </div>
                <nav>
                    <a href="#zavtraki" class="active">Завтраки</a>
                    <a href="#vypechka">Выпечка</a>
                    <a href="#sendvichi">Сэндвичи</a>
                    <a href="#salaty">Салаты</a>
                    <a href="#supy">Супы</a>
                    <a href="#osnovnye">Основные</a>
                    <a href="#deserty">Десерты</a>
                    <a href="#napitki">Напитки</a>
                </nav>
                <div class="cart">Корзина / <span id="cart-total">0 ₸</span></div>
            </div>
        </div>
    </header>

    <main class="container">
        <!-- Специальный блок для завтраков -->
        <section class="breakfast-special">
            <div class="container">
                <h2>🍳 Утреннее Наслаждение</h2>
                <p>Начните свой день с наших специальных завтраков</p>
                <div class="breakfast-time">с 8:00 до 12:00</div>
            </div>
        </section>

        <!-- Завтраки -->
        <section id="zavtraki" class="menu-category">
            <h2>ЗАВТРАКИ</h2>
            <div class="menu-items">
                <div class="menu-item">
                    <div class="menu-item-image">🥐</div>
                    <div class="menu-details">
                        <h3>Итальянский завтрак</h3>
                        <div class="description">Круассан, капучино и свежевыжатый апельсиновый сок</div>
                        <div class="item-footer">
                            <div class="price">2 500 ₸</div>
                            <button class="add-btn" data-price="2500">+</button>
                        </div>
                    </div>
                </div>
                <div class="menu-item">
                    <div class="menu-item-image">🥑</div>
                    <div class="menu-details">
                        <h3>Авокадо-тост</h3>
                        <div class="description">Хлеб на закваске с авокадо, яйцом пашот и рукколой</div>
                        <div class="item-footer">
                            <div class="price">2 800 ₸</div>
                            <button class="add-btn" data-price="2800">+</button>
                        </div>
                    </div>
                </div>
                <div class="menu-item">
                    <div class="menu-item-image">🍌</div>
                    <div class="menu-details">
                        <h3>Панкейки с ягодами</h3>
                        <div class="description">Воздушные панкейки со свежими ягодами и кленовым сиропом</div>
                        <div class="item-footer">
                            <div class="price">2 200 ₸</div>
                            <button class="add-btn" data-price="2200">+</button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Выпечка -->
        <section id="vypechka" class="menu-category">
            <h2>ВЫПЕЧКА</h2>
            <div class="menu-items">
                <div class="menu-item">
                    <div class="menu-item-image">🥖</div>
                    <div class="menu-details">
                        <h3>Круассан шоколадный</h3>
                        <div class="description">Слоеный круассан с начинкой из бельгийского шоколада</div>
                        <div class="item-footer">
                            <div class="price">900 ₸</div>
                            <button class="add-btn" data-price="900">+</button>
                        </div>
                    </div>
                </div>
                <div class="menu-item">
                    <div class="menu-item-image">🍞</div>
                    <div class="menu-details">
                        <h3>Бриошь с корицей</h3>
                        <div class="description">Нежная бриошь с ароматной корицей и глазурью</div>
                        <div class="item-footer">
                            <div class="price">750 ₸</div>
                            <button class="add-btn" data-price="750">+</button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Сэндвичи -->
        <section id="sendvichi" class="menu-category">
            <h2>СЭНДВИЧИ</h2>
            <div class="menu-items">
                <div class="menu-item">
                    <div class="menu-item-image">🥪</div>
                    <div class="menu-details">
                        <h3>Клаб сэндвич</h3>
                        <div class="description">Курица, бекон, салат, помидор и соус цезарь</div>
                        <div class="item-footer">
                            <div class="price">2 900 ₸</div>
                            <button class="add-btn" data-price="2900">+</button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Добавьте остальные секции по аналогии -->

    </main>

    <footer>
        <div class="container">
            <div class="footer-content">
                <h3>MUSETTI COFFEE & BAKERY</h3>
                <p class="contact-info">г. Алматы, ул. Гоголя, 15</p>
                <p class="contact-info">+7 (727) 345-67-89</p>
                <p class="hours">Часы работы: Ежедневно с 8:00 до 22:00</p>
            </div>
        </div>
    </footer>

    <script>
        // Корзина и расчеты
        let total = 0;
        const cartTotal = document.getElementById("cart-total");

        document.querySelectorAll(".add-btn").forEach(btn => {
            btn.addEventListener("click", () => {
                total += parseInt(btn.dataset.price);
                cartTotal.textContent = total.toLocaleString() + " ₸";
                
                // Анимация кнопки
                btn.style.transform = 'scale(0.9)';
                setTimeout(() => {
                    btn.style.transform = 'scale(1)';
                }, 150);
            });
        });

        // Плавная прокрутка для навигации
        document.querySelectorAll('nav a').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 100,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Подсветка активного раздела
        window.addEventListener('scroll', () => {
            const sections = document.querySelectorAll('.menu-category');
            const navLinks = document.querySelectorAll('nav a');
            
            let currentSection = '';
            
            sections.forEach(section => {
                const sectionTop = section.offsetTop - 120;
                if (window.scrollY >= sectionTop) {
                    currentSection = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === `#${currentSection}`) {
                    link.classList.add('active');
                }
            });
        });
    </script>
</body>
</html>